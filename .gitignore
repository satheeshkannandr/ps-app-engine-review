# Local, machine-specific Claude Code settings (paths, permissions) — do not publish
.claude/settings.local.json

# Internal AE review outputs — contain real instance data (hostnames, BU codes, logic).
# Keep these OUT of GitHub. Remove a line if you ever intentionally publish a review.
*_review.md
/LN_*/
/Reviews/

# WT-376748 freight-exempt investigation — real order/customer/invoice data + config
/WT376748_freight_exempt/

# Local doc-generation helper script — not part of the published repo
make_doc.py

# Playwright MCP scratch output (page snapshots/screenshots) — not part of the repo
.playwright-mcp/

# GitHub folder — kept local, not published
.github/

# Claude project instructions — kept local, not published
/CLAUDE.md

# Self use
/Commands.txt

# Gemini project instructions — kept local, not published
/GEMINI.md

# Agents for Gemini folder — kept local, not published
.agents/